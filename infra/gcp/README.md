# GCP Infrastructure Templates

Este directorio contiene plantillas para despliegue en Google Cloud.

## Archivos

- `cloudbuild.backend.yaml`: build y despliegue del backend a Cloud Run.
- `cloudbuild.frontend.yaml`: build y despliegue del frontend a Cloud Run.

## Documentación principal

Ver guía completa en:

- `../../docs/deployment-gcp.md`

